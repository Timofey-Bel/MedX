# Календарь ежедневных входов

## Функционал
- Недельные слайды (7 дней в сетке)
- Автоматическая отметка текущего дня
- Визуальные индикаторы: ✓ посещен, ✗ пропущен, ○ нейтральный
- Навигация стрелками между неделями
- Сохранение в localStorage

## Логика крестиков
Крестики появляются только для дней **после первого входа** пользователя. Дни до регистрации не помечаются.

Условия для крестика:
1. День в прошлом
2. День не был посещен
3. День >= дате первого входа

## Сохраняемые данные

```javascript
// Дата первого входа
localStorage: medx_first_visit_date
Формат: "2026-03-20T00:00:00.000Z"

// Посещенные дни
localStorage: medx_visited_days
Формат: ["2026-03-27", "2026-03-26"]
```

## Примеры

### Новый пользователь (первый вход сегодня)
```
20.03 - точка (до регистрации)
21.03 - точка (до регистрации)
...
27.03 - галочка + синяя точка (сегодня)
```

### Пользователь неделю назад (посещения: 20, 21, 23, 27 марта)
```
20.03 - галочка (первый вход)
21.03 - галочка
22.03 - крестик (пропущен)
23.03 - галочка
24.03 - крестик (пропущен)
25.03 - крестик (пропущен)
26.03 - крестик (пропущен)
27.03 - галочка + синяя точка (сегодня)
```

## Тестирование

### Быстрый тест
1. Откройте `http://localhost/test-calendar.html`
2. Проверьте отображение 7 дней в сетке
3. Используйте кнопку "Установить первый вход неделю назад"

### Консольные команды
```javascript
// Посмотреть данные
localStorage.getItem('medx_visited_days')
localStorage.getItem('medx_first_visit_date')

// Очистить
localStorage.removeItem('medx_visited_days')
localStorage.removeItem('medx_first_visit_date')
location.reload()

// Симуляция входа неделю назад
const weekAgo = new Date()
weekAgo.setDate(weekAgo.getDate() - 7)
localStorage.setItem('medx_first_visit_date', weekAgo.toISOString())
location.reload()
```

## Файлы
- `public/js/daily-calendar.js` - логика календаря
- `public/css/main_styles.css` - стили
- `resources/views/main_showcase.blade.php` - HTML
- `public/test-calendar.html` - тестовая страница
